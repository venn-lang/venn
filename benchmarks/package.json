{
  "name": "@venn-lang/benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "node src/main.ts",
    "profile": "node --cpu-prof --cpu-prof-dir=prof-out src/profile/run-case.ts",
    "profile:top": "node src/profile/top.mjs prof-out",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@venn-lang/contracts": "workspace:*",
    "@venn-lang/core": "workspace:*",
    "@venn-lang/runtime": "workspace:*",
    "@venn-lang/stdlib": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
