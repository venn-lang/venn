# differs fnDecl a `fn` is pure, so it may not call a verb where the top of a file may
# differs fnExpr a `fn` is pure, so it may not call a verb where the top of a file may
let stop = fail "the guard fired"
seen = "${seen}fell through"
