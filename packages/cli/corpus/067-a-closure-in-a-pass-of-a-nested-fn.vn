let build = fn (base) {
  let out = []
  repeat 2 as i {
    out = out.push(fn () => base * i)
  }
  return out
}
let got = build(10)
let vals = []
forEach one in got {
  vals = vals.push(one())
}
seen = vals.join(" ")
