# excludes fnDecl a `namespace` is a declaration, and a pure body holds statements
# excludes fnExpr a `namespace` is a declaration, and a pure body holds statements
# excludes fragment a `namespace` is a declaration, and a fragment body holds statements
namespace grouped {
  flow "inside a namespace" {
    io.print "the flow ran"
  }
}
seen = "${seen}end"
