let doubled = "outer"
let made = []
forEach n in [1, 2, 3] {
  let doubled = n * 2
  made = made.push(fn () => doubled)
}
let out = []
forEach one in made {
  out = out.push(one())
}
let shown = out.join(" ")
seen = "${shown} outer=${doubled}"
